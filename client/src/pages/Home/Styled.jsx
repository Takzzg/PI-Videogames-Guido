import styled from "styled-components"

const Styled = styled.div`
    display: flex;

    position: relative;
    min-height: 0;

    .searchField {
        position: sticky;
        top: 0;
    }
`

export { Styled }
